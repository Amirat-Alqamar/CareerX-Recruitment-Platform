# 📘 مرجع قاعدة البيانات والموديلات والعلاقات الشامل - منصة CareerX

مرجع شامل لجميع جداول وموديلات المنصة الـ 17 والعلاقات التبادلية بينها.

---

## 🗂️ أولاً: دليل جداول قاعدة البيانات والحقول (Database Tables & Fields)

### 1. جداول الحسابات والشركات (Users & Companies)

| اسم الجدول في الداتابيز | الحقول الأساسية ومترجمة بالعربية | الشرح والوظيفة |
| :--- | :--- | :--- |
| **`users`** | `name` (الاسم الكامل)، `email` (الإيميل)، `password` (كلمة السر)، `role` (نوع الحساب)، `company_id` (رقم الشركة)، `phone` (رقم الهاتف)، `status` (حالة الحساب) | الجدول الرئيسي لحسابات المستخدمين |
| **`companies`** | `name` (اسم الشركة)، `slug` (الرابط الفريد)، `logo` (الشعار)، `description` (الوصف)، `website` (الموقع الإلكتروني)، `company_size` (حجم الشركة)، `address` (العنوان) | معلومات وتفاصيل الشركات |
| **`company_socials`** | `company_id` (رقم الشركة)، `platform` (اسم المنصة مثل LinkedIn)، `url` (الرابط) | روابط شبكات التواصل الاجتماعي للشركة |

---

### 2. جداول بروفايل الباحث عن عمل (Job Seeker Profile)

| اسم الجدول في الداتابيز | الحقول الأساسية ومترجمة بالعربية | الشرح والوظيفة |
| :--- | :--- | :--- |
| **`job_seeker_profiles`** | `user_id` (رقم المستخدم)، `job_title` (المسمى الوظيفي)، `bio` (النبذة)، `years_of_experience` (سنوات الخبرة)، `nationality` (الجنسية)، `birth_date` (تاريخ الميلاد)، `work_type` (نوع العمل)، `country_id` / `city_id` | البروفايل المهني والأساسي للباحث عن عمل |
| **`experiences`** | `profile_id` (رقم البروفايل)، `company_name` (اسم الشركة)، `title` (المسمى الوظيفي)، `start_date` / `end_date` (التواريخ)، `description` (الوصف والمهام) | الخبرات المهنية السابقة للباحث |
| **`education`** | `profile_id` (رقم البروفايل)، `university` (الجامعة أو المعهد)، `degree` (التخصص/الدرجة)، `gpa` (المعدل)، `start_year` / `end_year` (سنوات الدراسة والتخرج) | المؤهلات التعليمية والجامعية |
| **`resumes`** | `profile_id` (رقم البروفايل)، `title` (عنوان السيرة الذاتية)، `file_path` (مسار ملف الـ CV المخزن) | ملفات السير الذاتية المرفوعة |
| **`certifications`** | `profile_id` (رقم البروفايل)، `name` (اسم الشهادة)، `issuer` (الجهة المانحة)، `issue_date` (تاريخ الإصدار)، `credential_url` (رابط التوثيق) | الشهادات المهنية والدورات التدريبية |
| **`portfolio_items`** | `profile_id` (رقم البروفايل)، `title` (عنوان العمل)، `type` (نوع المعرض)، `url` (الرابط)، `file_path` (مسار الملف) | معرض أعمال الباحث المهني (Portfolio) |
| **`projects`** | `profile_id` (رقم البروفايل)، `title` (عنوان المشروع)، `description` (الشرح)، `url` (رابط المشروع) | المشاريع الشخصية والتطبيقات |

---

### 3. جداول المهارات واللغات (Skills & Languages)

| اسم الجدول في الداتابيز | الحقول الأساسية ومترجمة بالعربية | الشرح والوظيفة |
| :--- | :--- | :--- |
| **`skills`** | `name` (اسم المهارة مثل: Laravel, React) | جدول المهارات العامة بالمنصة |
| **`languages`** | `name` (اسم اللغة مثل: العربية، الإنجليزية) | جدول اللغات العامة بالمنصة |
| **`profiles_skills`** | `profile_id` (رقم البروفايل)، `skill_id` (رقم المهارة)، `level_id` (مستوى المهارة من 1 إلى 5) | الجدول الوسيط لربط مهارات الباحث |
| **`profiles_languages`** | `profile_id` (رقم البروفايل)، `language_id` (رقم اللغة)، `level` (مستوى الإتقان) | الجدول الوسيط لربط لغات الباحث |

---

### 4. جداول الوظائف والتقديمات والدول (Jobs & Applications)

| اسم الجدول في الداتابيز | الحقول الأساسية ومترجمة بالعربية | الشرح والوظيفة |
| :--- | :--- | :--- |
| **`job_posts`** | `company_id` (رقم الشركة الناشرة)، `created_by_user_id` (المستخدم الناشر)، `category_id` (القسم)، `title` (عنوان الوظيفة)، `description` (التفاصيل)، `salary_min` / `salary_max` (الراتب)، `job_type` (نوع الدوام)، `status` (حالة الوظيفة) | إعلانات الوظائف المنشورة من الشركات |
| **`job_categories`** | `name` (اسم القسم مثل: برمجيات، تسويق)، `slug` (الرابط الفريد) | تصنيفات وأقسام الوظائف بالمنصة |
| **`job_applications`** | `job_post_id` (رقم الوظيفة)، `profile_id` (رقم المتقدم)، `resume_id` (الـ CV المرفق)، `cover_letter` (رسالة التغطية)، `status` (حالة الطلب) | طلبات تقديم الباحثين على الوظائف |
| **`countries` / `cities`** | `name` (اسم الدولة/المدينة)، `country_id` (رقم الدولة) | الدول والمدن المتاحة بالنظام |

---

## 🔄 ثانياً: مرجع علاقات الموديلات التبادلي الكامل (Models & 2-Way Relationships)

| الموديل الأول (Model A) | الموديل الثاني (Model B) | علاقة (Model A ➡ Model B) | العلاقة العكسية (Model B ➡ Model A) | نوع العلاقة وشرحها |
| :--- | :--- | :--- | :--- | :--- |
| **`User`** | **`JobSeekerProfile`** | `hasOne` (يملك بروفايل واحد) | `belongsTo` (ينتمي لمستخدم واحد) | **1 إلى 1** (حساب وبروفايل) |
| **`Company`** | **`User`** | `hasMany` (تضم عدة موظفين) | `belongsTo` (ينتمي لشركة واحدة) | **1 إلى متعدد** (شركة وموظفيها) |
| **`Company`** | **`CompanySocial`** | `hasMany` (تملك عدة روابط) | `belongsTo` (تنتمي لشركة واحدة) | **1 إلى متعدد** (شركة وروابط تواصل) |
| **`JobSeekerProfile`** | **`Experience`** | `hasMany` (يملك عدة خبرات) | `belongsTo` (تنتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل وخبرات سابقة) |
| **`JobSeekerProfile`** | **`Education`** | `hasMany` (يملك عدة شهادات) | `belongsTo` (ينتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل ومؤهلات تعليمية) |
| **`JobSeekerProfile`** | **`Resume`** | `hasMany` (يملك عدة ملفات CV) | `belongsTo` (تنتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل وسير ذاتية) |
| **`JobSeekerProfile`** | **`Certification`** | `hasMany` (يملك عدة شهادات) | `belongsTo` (تنتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل وشهادات مهنية) |
| **`JobSeekerProfile`** | **`PortfolioItem`** | `hasMany` (يملك عدة أعمال) | `belongsTo` (ينتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل وأعمال معرض) |
| **`JobSeekerProfile`** | **`Project`** | `hasMany` (يملك عدة مشاريع) | `belongsTo` (ينتمي لبروفايل واحد) | **1 إلى متعدد** (بروفايل ومشاريع) |
| **`JobSeekerProfile`** | **`JobApplication`** | `hasMany` (قدم على عدة طلبات) | `belongsTo` (ينتمي لمتقدم واحد) | **1 إلى متعدد** (متقدم وطلبات توظيف) |
| **`JobSeekerProfile`** | **`Skill`** | `belongsToMany` (يملك عدة مهارات) | `belongsToMany` (المهارة يملكها عدة باحثين) | **متعدد إلى متعدد** (عبر `profiles_skills`) |
| **`JobSeekerProfile`** | **`Language`** | `belongsToMany` (يتحدث عدة لغات) | `belongsToMany` (اللغة يتحدثها عدة باحثين) | **متعدد إلى متعدد** (عبر `profiles_languages`) |
| **`Company`** | **`Job`** *(job_posts)* | `hasMany` (تنشر عدة وظائف) | `belongsTo` (تنتمي لشركة واحدة) | **1 إلى متعدد** (شركة وإعلانات وظائف) |
| **`JobCategory`** | **`Job`** *(job_posts)* | `hasMany` (يحتوي عدة وظائف) | `belongsTo` (تنتمي لتصنيف واحد) | **1 إلى متعدد** (قسم وإعلانات وظائف) |
| **`Job`** *(job_posts)* | **`JobApplication`** | `hasMany` (تتلقى عدة طلبات) | `belongsTo` (ينتمي لوظيفة واحدة) | **1 إلى متعدد** (إعلان وظيفة وطلبات التقديم) |
| **`Job`** *(job_posts)* | **`Skill`** | `belongsToMany` (تتطلب عدة مهارات) | `belongsToMany` (المهارة مطلوبة بعدة وظائف) | **متعدد إلى متعدد** (عبر `job_skill`) |
| **`Country`** | **`City`** | `hasMany` (تضم عدة مدن) | `belongsTo` (تنتمي لدولة واحدة) | **1 إلى متعدد** (دولة ومدن) |
